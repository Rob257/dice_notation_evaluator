<%= form_for(@admin_account) do |f| %>
  <% if @admin_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_account.errors.count, "error") %> prohibited this admin_account from being saved:</h2>

      <ul>
      <% @admin_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :admin_account_password, 'Password' %><br />
    <%= f.password_field :password, :size => 40 %>
  </div>
  <div class="field">
    <%= f.label :admin_account_password_confirmation, 'Confirm' %><br />
    <%= f.password_field :password_confirmation, :size => 40 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

